{
  "name": "merdeka-build-tools",
  "private": true,
  "version": "1.0.0",
  "description": "Tool build lokal untuk regenerate js/app.bundle.min.js, icons/lucide-icons.local.min.js, dan style.min.css. Situs tetap dideploy sebagai file statis biasa (lihat wrangler.jsonc) — ini cuma dijalankan manual sebelum push/deploy.",
  "scripts": {
    "build": "node build.js"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
